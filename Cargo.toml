[package]
name = "rusty_gui"
version = "0.1.0"
edition = "2024"
description = "This is a simple GUI library for Rust."
license = "Apache-2.0"
repository = "https://github.com/Anglebase/Rusty_GUI.git"

[dependencies]

[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3", features = ["winuser", "libloaderapi", "wingdi", "errhandlingapi"] }